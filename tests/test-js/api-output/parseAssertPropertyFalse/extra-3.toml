instructions = [
  """let parseAssertElemProp = await page.$(\"a\");
if (parseAssertElemProp === null) { throw '\"a\" not found'; }
await page.evaluate(e => {
    const nonMatchingProps = [];
    const parseAssertElemPropDict = {\"a\":\"1\"};
    const undefProps = [];
    for (const prop of undefProps) {
        if (e[prop] !== undefined && e[prop] !== null) {
            continue;
        }
        nonMatchingProps.push(\"Property named `\" + prop + \"` doesn't exist\");
    }
    for (const [parseAssertElemPropKey, parseAssertElemPropValue] of Object.entries(parseAssertElemPropDict)) {
        if (e[parseAssertElemPropKey] === undefined || e[parseAssertElemPropKey] === null) {
            continue;
        }
        const prop = String(e[parseAssertElemPropKey]);
        if (prop.startsWith(parseAssertElemPropValue)) {
            nonMatchingProps.push(\"assert didn't fail for property `\" + parseAssertElemPropKey + \"` (`\" + prop + \"`) (for STARTS_WITH check)\");
        }
        if (prop.endsWith(parseAssertElemPropValue)) {
            nonMatchingProps.push(\"assert didn't fail for property `\" + parseAssertElemPropKey + \"` (`\" + prop + \"`) (for ENDS_WITH check)\");
        }
    }
    if (nonMatchingProps.length !== 0) {
        const props = nonMatchingProps.join(\"; \");
        throw \"The following errors happened (for selector `a`): [\" + props + \"]\";
    }
}, parseAssertElemProp);""",
]
wait = false
warnings = [
]
checkResult = true
