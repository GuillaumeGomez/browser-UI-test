{
  "instructions": [
    "let parseClickVar = await page.$(\"a\");\nif (parseClickVar === null) { throw '\"a\" not found'; }\nawait parseClickVar.click();"
  ],
  "warnings": [
    "Pseudo-elements (`::after`) can't be retrieved so `click` will be performed on the element directly"
  ]
}
