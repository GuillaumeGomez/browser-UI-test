instructions = [
  """const check = 1 > 2 + 3;
try {
if (!check) {
    throw \"Condition `1 > 2 + 3` was evaluated as false\";
}
} catch(e) { return; } throw \"assert didn't fail\";""",
]
wait = false
checkResult = true
