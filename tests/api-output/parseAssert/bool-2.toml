instructions = [
  """const { compareArrayLike, compareJson } = require('command-helpers.js');

const check = 1 >= 2 / (9);
if (!check) {
    throw \"Condition `1 >= 2 / (3 + 6)` (`1 >= 2 / (9)`) was evaluated as false\";
}""",
]
wait = false
checkResult = true
