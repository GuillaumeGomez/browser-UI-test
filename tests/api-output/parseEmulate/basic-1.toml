instructions = [
  """if (arg.puppeteer.KnownDevices[\"a\"] === undefined) {
    throw 'Unknown device `a`. List of available devices can be found there: https://github.com/GoogleChrome/puppeteer/blob/master/lib/DeviceDescriptors.js or you can use `--show-devices` option';
} else {
    await page.emulate(arg.puppeteer.KnownDevices[\"a\"]);
}""",
]
