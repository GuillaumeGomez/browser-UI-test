instructions = [
  """await page.evaluate(() => {
    const found = window.find(
        \"a\",
        false,
        false,
        true,
        true,
        true,
        false,
    );
    if (!found) {
        throw new Error(\"Didn't find text `\" + \"a\" + \"`\");
    }
});""",
]
wait = false
checkResult = true
