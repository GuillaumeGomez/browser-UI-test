instructions = [
  """const timeLimit = pages[0].getDefaultTimeout();
const timeAdd = 50;
let allTime = 0;
let assertSize = null;
while (true) {
    while (true) {
        assertSize = await page.$$(\"::-p-xpath(//a)\");
        if (assertSize.length !== 0) {
            assertSize = assertSize[0];
            break;
        }
        await new Promise(r => setTimeout(r, timeAdd));
        if (timeLimit === 0) {
            continue;
        }
        allTime += timeAdd;
        if (allTime >= timeLimit) {
            throw new Error(\"The XPath \\\"//a\\\" was not found\");
        }
    }
    let erroredElem = null;
    async function checkElemSize(elem) {
        return await elem.evaluate(e => {
            const innerErrors = [];
            const height = e.offsetHeight;
            const width = e.offsetWidth;

            return innerErrors;
        });
    }
    const errors = [];
    errors.push(...await checkElemSize(assertSize));
    if (errors.length !== 0) {
        break;
    }

    await new Promise(r => setTimeout(r, timeAdd));
    if (timeLimit === 0) {
        continue;
    }
    allTime += timeAdd;
    if (allTime >= timeLimit) {
        let err = \"All checks still pass\";
        throw new Error(err);
    }
}""",
]
wait = false
warnings = [
]
checkResult = true
