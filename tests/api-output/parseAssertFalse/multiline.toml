instructions = [
  """try {
if ((await page.$(\"::-p-xpath(//a)\")) === null) {
    throw '\"//a\" not found';
}
} catch(e) { return; } throw \"assert didn't fail\";""",
]
wait = false
checkResult = true
